---
const { style } = Astro.props;
---

<canvas id="idParticles" class={style}></canvas>

<script >
  // @ts-ignore
  import Particles from "particlesjs" ;

  window.onload = () => {
    Particles.init({
      selector: '#idParticles',
      maxParticles: 140,
      speed: 1,
      color: "#ffffff",
      minDistance:50,
      connectParticles: true
    });
  };
</script>

